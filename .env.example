# API Keys for AI
GOOGLE_AI_API_KEY=your_gemini_api_key_here

# API Keys for the image tool
GOOGLE_CUSTOM_API_KEY=your_google_custom_search_api_key_here

# Google Custom Search Engine ID
# Get this from: https://programmablesearchengine.google.com/
# Create a new search engine and copy the "Search engine ID" (cx parameter)
GOOGLE_CX=your_search_engine_id_here

# Gemini Evaluation
USE_GEMINI_EVAL=true

# Image Search Configuration
# Image Size: huge, icon, large, medium, small, xlarge, xxlarge
IMG_SIZE=huge

# Image Type: clipart, face, lineart, photo, animated
IMG_TYPE=photo

# Number of search results to fetch per keyword (max 100, uses pagination)
# Values > 10 will automatically make multiple API requests
# Examples: 5 (1 request), 15 (2 requests), 30 (3 requests)
NUM_RESULTS=15

# Keyword Processing Range - Choose ONE method:

# Method 1: Process by Part Number (RECOMMENDED)
# Process all keywords in specific parts (comma-separated)
# Examples:
#   PROCESS_PARTS=1       → All part 1 keywords (1-1, 1-2, ..., 1-30)
#   PROCESS_PARTS=2,3     → All part 2 and 3 keywords
#   PROCESS_PARTS=1,2,3,4,5,6  → All keywords from parts 1-6
PROCESS_PARTS=3,4,5,6

# Method 2: Process by Specific IDs
# Specify exact IDs or ID ranges (comma-separated)
# Ranges use colon notation: start:end (inclusive)
# Examples:
#   PROCESS_IDS=1-1,1-5,1-10     → Only these 3 specific keywords
#   PROCESS_IDS=2-1:2-10          → Keywords 2-1 through 2-10
#   PROCESS_IDS=3-1:3-5,4-1:4-5   → Parts 3 and 4, only IDs 1-5
# PROCESS_IDS=3-1:3-5

# Method 3: Process by Array Index (OLD METHOD, for backward compatibility)
# Use array indices (0-based). Less readable due to missing IDs.
# START_INDEX=0
# END_INDEX=80

# Optional: Color Type (color, gray, mono, trans)
# IMG_COLOR_TYPE=color

# Optional: Dominant Color (black, blue, brown, gray, green, orange, pink, purple, red, teal, white, yellow)
# IMG_DOMINANT_COLOR=

# Optional: File Type (jpg, gif, png, bmp, svg, webp, ico)
# FILE_TYPE=jpg
